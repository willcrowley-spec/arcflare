services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: arcflare
      POSTGRES_PASSWORD: arcflare
      POSTGRES_DB: arcflare
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U arcflare -d arcflare"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  pgdata:
